/*
 * SearchResults
 *
 * This is the first thing users see of our App, at the '/' route
 *
 */

import React from 'react';

export default class SearchResults extends React.Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <h1>
        Test
      </h1>
    );
  }
}
