// import { fromJS } from 'immutable';
// import { makeSelectDepartmentListDomain } from '../selectors';

// const selector = makeSelectDepartmentListDomain();

describe('makeSelectDepartmentListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
