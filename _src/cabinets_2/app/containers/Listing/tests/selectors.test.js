// import { fromJS } from 'immutable';
// import { makeSelectListingDomain } from '../selectors';

// const selector = makeSelectListingDomain();

describe('makeSelectListingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
