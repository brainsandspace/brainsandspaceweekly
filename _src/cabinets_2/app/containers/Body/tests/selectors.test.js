// import { fromJS } from 'immutable';
// import { makeSelectBodyDomain } from '../selectors';

// const selector = makeSelectBodyDomain();

describe('makeSelectBodyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
